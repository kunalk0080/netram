# MongoDB Atlas connection string
MONGO_URI=mongodb://127.0.0.1:27017/nayanaa

# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Firebase Admin — paste the full service account JSON on one line (optional in dev).
# If left blank, token verification is skipped and a dev fallback user is used.
FIREBASE_SERVICE_ACCOUNT_JSON=
